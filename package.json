{
  "name": "get-token",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "dot-env": "^0.0.1"
  },
  "devDependencies": {
    "twilio-run": "^3.4.2"
  }
}
